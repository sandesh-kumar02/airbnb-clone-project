<% layout('layouts/boilerplate') %> <% title = 'Listing Details' %> <% bodyClass
= 'show-listing-page' %>

<!-- ================= Leaflet CSS ================= -->
<link
  rel="stylesheet"
  href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
  integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
  crossorigin=""
/>

<div class="container py-5">
  <div class="mx-auto" style="max-width: 720px">
    <!-- Card -->
    <div class="card shadow-lg rounded-4">
      <!-- Image -->
      <img
        src="<%= showListing.image.url %>"
        class="card-img-top"
        alt="listing image"
      />

      <!-- Card body -->
      <div class="card-body">
        <h3 class="card-title mb-3"><%= showListing.title %></h3>
        <i>Owned By <%= showListing.owner.username %></i>

        <p class="card-text text-muted"><%= showListing.description %></p>

        <p class="card-text fw-bold">
          &#8377; <%= showListing.price.toLocaleString('en-IN') %> / night
        </p>

        <p class="card-text">
          <i class="fa-solid fa-location-dot"></i>
          <%= showListing.location %>, <%= showListing.country %>
        </p>
      </div>

      <!-- Card footer -->
      <% if (currUser && showListing.owner._id.equals(currUser._id)) { %>
      <div class="card-footer d-flex justify-content-between">
        <a href="/listings/<%= showListing._id %>/edit" class="btn btn-warning"
          >✏️ Edit</a
        >

        <form
          action="/listings/<%= showListing._id %>?_method=DELETE"
          method="POST"
        >
          <button class="btn btn-danger">🗑 Delete</button>
        </form>
      </div>
      <% } %>
    </div>
  </div>
</div>

<!-- ================= MAP ================= -->
<div class="mt-5 offset-2">
  <div id="map" style="height: 350px; border-radius: 12px"></div>
</div>

<!-- ================= REVIEWS ================= -->
<div class="mt-5">
  <hr />

  <!-- ADD REVIEW -->
  <% if (currUser) { %>
  <h4 class="mb-4">Leave a Review</h4>

  <form
    action="/listings/<%= showListing._id %>/reviews"
    class="needs-validation"
    novalidate
    method="POST"
  >
    <div class="form-text">Rate from 1 (worst) to 5 (best)</div>

    <div class="mb-3 mt-3">
      <label class="form-label fw-semibold">Rating</label>

      <fieldset class="starability-slot">
        <legend>First rating:</legend>

        <input
          type="radio"
          id="no-rate"
          class="input-no-rate"
          name="review[rating]"
          value="1"
          checked
          aria-label="No rating."
        />

        <input type="radio" id="first-rate1" name="review[rating]" value="1" />
        <label for="first-rate1" title="Terrible">1 star</label>

        <input type="radio" id="first-rate2" name="review[rating]" value="2" />
        <label for="first-rate2" title="Not good">2 stars</label>

        <input type="radio" id="first-rate3" name="review[rating]" value="3" />
        <label for="first-rate3" title="Average">3 stars</label>

        <input type="radio" id="first-rate4" name="review[rating]" value="4" />
        <label for="first-rate4" title="Very good">4 stars</label>

        <input type="radio" id="first-rate5" name="review[rating]" value="5" />
        <label for="first-rate5" title="Amazing">5 stars</label>
      </fieldset>
    </div>

    <div class="mb-3">
      <label class="form-label fw-semibold">Comment</label>
      <textarea
        name="review[comment]"
        rows="4"
        class="form-control"
        placeholder="Write your review here..."
        required
      ></textarea>
    </div>

    <div class="d-grid">
      <button class="btn btn-primary btn-lg rounded-3">💬 Submit Review</button>
    </div>
  </form>
  <% } %>

  <hr />

  <!-- SHOW REVIEWS -->
  <h4 class="mb-4">Show Reviews</h4>

  <div class="list-group">
    <% if (showListing.reviews.length === 0) { %>
    <p class="text-muted fst-italic">No reviews yet. Be the first to review!</p>
    <% } else { %> <% for (const review of showListing.reviews) { %>
    <div
      class="list-group-item list-group-item-action shadow-sm mb-3 rounded-3"
    >
      <h5>@<%= review.author.username %></h5>

      <!-- ⭐ Read-only stars -->
      <p class="starability-result" data-rating="<%= review.rating %>"></p>

      <div class="d-flex justify-content-between align-items-center mb-2">
        <span class="fw-semibold"> ⭐ Rating: <%= review.rating %>/5 </span>
        <small class="text-muted">
          <i class="fa-regular fa-clock"></i> just now
        </small>
      </div>

      <p class="mb-1"><%= review.comment %></p>

      <% if (currUser && review.author._id.equals(currUser._id)) { %>
      <form
        action="/listing/<%= showListing._id %>/reviews/<%= review._id %>?_method=DELETE"
        method="POST"
      >
        <button class="btn btn-sm btn-outline-danger">delete</button>
      </form>
      <% } %>
    </div>
    <% } %> <% } %>
  </div>
</div>

<!-- ================= Leaflet JS ================= -->
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

<!-- 🔥 GeoJSON coordinates (PEHLE) -->
<script>
  // MongoDB GeoJSON → [lng, lat]
  const coordinates = <%- JSON.stringify(showListing.geometry.coordinates) %>;
</script>

<!-- 🔥 Map logic (BAAD ME) -->
<script src="/js/map.js"></script>
